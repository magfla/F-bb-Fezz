function Points(asteroider){
	// TANKEN er å vise poengsummen som en funksjon av antall kollisjoner 
	// laseren og asteroidene har 
		textAlign(CENTER)
		textSize(22);
		text(asteroider.length,20,0);
}